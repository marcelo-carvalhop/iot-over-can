# Release v0.13.0 — descoberta BLE distribuída

Entregue: advertising BLE autônomo no Pico W; scan BLE em cada Node CAN funcional;
RSSI por Node; relatório em dois frames CAN clássico; reagrupamento na Probe 00;
candidatos visíveis por Node na TUI; `pico_w` como placa padrão de build.

A associação e a telemetria wireless através do Node escolhido permanecem para a
próxima etapa.
